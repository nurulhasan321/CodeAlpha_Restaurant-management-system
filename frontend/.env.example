# Savory Frontend — Environment Configuration
# Copy this file to .env.local and fill in your values

# Backend API base URL (no trailing slash)
# Development (default — points to local Spring Boot instance):
VITE_API_BASE_URL=http://localhost:8080

# Production example:
# VITE_API_BASE_URL=https://api.savory.yourdomain.com
